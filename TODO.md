# Hello AI! RAG App TODO

## 方針メモ

- 当面は `InMemoryVectorStore` のままで OK。小規模データのデモやポートフォリオ用途なら十分に実用的。
- Web アプリとして継続運用するなら、将来的には永続化されたベクトルストアへ移行した方がよい。
- ただし、今の Windows ローカル環境では埋め込み型 `Chroma` がクラッシュしていたため、すぐに戻すのは非推奨。
- 永続化を再導入するなら、優先順位は `Chroma Server/Docker`、`Qdrant`、`pgvector` などの安定運用しやすい構成を検討する。

## 最優先

- [ ] `.env.example` を追加して、公開用の安全な環境変数テンプレートを作る
- [ ] `README.md` に本番向けセットアップ手順を追記する
- [ ] FAQ データの更新手順を整理し、`faq_data.txt` 差し替え時の運用ルールを決める
- [ ] エラー時にユーザーへ分かりやすいメッセージを出すようにする
- [ ] API キー未設定、埋め込み失敗、LLM 応答失敗の例外ハンドリングを追加する

## 近いうちにやる

- [ ] FAQ 読み込み処理を関数分割して、`loader` / `retriever` / `chain` の責務を分ける
- [ ] `app.py` を 1 ファイル構成から分割し、保守しやすくする
- [ ] `Hello AI!` に関するサンプル質問を UI に表示する
- [ ] 回答と一緒に参照した FAQ の抜粋を表示できるようにする
- [ ] 回答が FAQ に無い場合の fallback 文言を改善する
- [ ] `st.cache_resource` / `st.cache_data` の使い分けを見直す

## 永続ベクトルストア移行

- [ ] ベクトルストアを抽象化して、実装を差し替え可能にする
- [ ] `InMemoryVectorStore` と永続ストアを設定で切り替えられるようにする
- [ ] `Chroma` を再検証する場合は、Windows ローカル埋め込みではなく `server mode` か Docker で検証する
- [ ] 代替候補として `Qdrant` / `pgvector` / `Pinecone` の比較表を作る
- [ ] データ登録処理をアプリ起動時実行から分離して、事前インデックス作成ジョブに移す
- [ ] FAQ 更新時に再インデックスするスクリプトを追加する

## 品質向上

- [ ] `pytest` を導入して、分割処理・プロンプト生成・検索処理のテストを書く
- [ ] 質問パターンごとの簡易評価データセットを作る
- [ ] 回答精度の観点で `chunk_size` / `chunk_overlap` / `k` のチューニングを行う
- [ ] ログ出力を追加して、質問内容・取得件数・例外内容を確認できるようにする
- [ ] `LangSmith` などでトレースできるようにする

## Web アプリ運用準備

- [ ] Streamlit Community Cloud、Render、Railway などのデプロイ先を決める
- [ ] 本番環境では `requirements.txt` のバージョンを固定寄りにして再現性を上げる
- [ ] 秘密情報を環境変数またはシークレット管理に寄せる
- [ ] 利用制限やレート制限を検討する
- [ ] 利用ログと監視方法を決める
- [ ] 将来的に認証が必要ならログイン機能を追加する

## あると良い改善

- [ ] チャット履歴をファイルまたは DB に保存できるようにする
- [ ] FAQ データを `.txt` だけでなく `.md` や `.csv` でも読めるようにする
- [ ] 管理画面から FAQ を追加・更新できるようにする
- [ ] 回答に信頼度や参照元メタデータを表示する
- [ ] スマホ表示を意識して UI を整える
